CREATE TABLE accounts_decks (
	username TEXT,
	name TEXT,
	id INTEGER,
	wins INTEGER,
	register INTEGER);
CREATE TABLE accounts_decks_items (
	username TEXT,
	id INTEGER,
	cardid INTEGER,
	amount INTEGER);
