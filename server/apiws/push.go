package apiws

// func pushJSON(socket *websocket.T, uri string, data map[string]any) {
// 	socket.Write(cast.BytesS(update.Build(uri, data).String()))
// }

// func pushError(socket *websocket.T, error string) {
// 	pushJSON(socket, "/error", map[string]any{
// 		"error": error,
// 	})
// }
